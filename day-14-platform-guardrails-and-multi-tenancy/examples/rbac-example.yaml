apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: team-a
  name: developer-role

rules:
  - apiGroups: [""]
    resources:
      - pods
      - services

    verbs:
      - get
      - list
      - watch
