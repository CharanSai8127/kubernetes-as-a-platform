apiVersion: v1
kind: LimitRange
metadata:
  name: team-a-limits
  namespace: team-a

spec:
  limits:
    - default:
        cpu: "1"
        memory: "1Gi"

      defaultRequest:
        cpu: "250m"
        memory: "256Mi"

      type: Container
