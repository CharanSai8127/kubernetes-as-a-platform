apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-environment

spec:
  replicas: 4

  selector:
    matchLabels:
      app: backend
      version: blue

  template:
    metadata:
      labels:
        app: backend
        version: blue

    spec:
      containers:
        - name: backend
          image: backend:v1
